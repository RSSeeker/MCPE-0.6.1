name: Build MCPE 0.6.1

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Fix PlatformToolset (v145 -> v143)
      shell: powershell
      run: |
        (Get-Content "handheld\project\win32\Debug\MinecraftWin32.vcxproj") -replace '<PlatformToolset>v145</PlatformToolset>', '<PlatformToolset>v143</PlatformToolset>' | Set-Content "handheld\project\win32\Debug\MinecraftWin32.vcxproj"

    - name: Build Debug
      shell: powershell
      run: |
        msbuild "handheld\project\win32\Debug\MinecraftWin32.vcxproj" /p:Configuration=Debug /p:Platform=Win32 /m

    - name: Build Release
      shell: powershell
      run: |
        msbuild "handheld\project\win32\Debug\MinecraftWin32.vcxproj" /p:Configuration=Release /p:Platform=Win32 /m

    - name: Copy DLLs to Release
      shell: powershell
      run: |
        $releaseDir = "handheld\project\win32\Debug\Release"
        $libBin = "handheld\lib\bin"
        Copy-Item "$libBin\libEGL.dll" $releaseDir -ErrorAction SilentlyContinue
        Copy-Item "$libBin\libgles_cm.dll" $releaseDir -ErrorAction SilentlyContinue
        Copy-Item "$libBin\libpng12.dll" $releaseDir -ErrorAction SilentlyContinue
        Copy-Item "$libBin\zlib1.dll" $releaseDir -ErrorAction SilentlyContinue

    - name: Upload Debug
      uses: actions/upload-artifact@v4
      with:
        name: MinecraftWin32-Debug
        path: |
          handheld\project\win32\Debug\MinecraftWin32.exe
          handheld\project\win32\Debug\*.dll

    - name: Upload Release
      uses: actions/upload-artifact@v4
      with:
        name: MinecraftWin32-Release
        path: |
          handheld\project\win32\Debug\Release\MinecraftWin32.exe
          handheld\project\win32\Debug\Release\*.dll